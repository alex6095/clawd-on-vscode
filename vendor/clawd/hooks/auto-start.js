#!/usr/bin/env node
// Clawd on VS Code runs inside the VS Code extension host, so a shell hook
// cannot safely launch it like the desktop app. This stub intentionally keeps
// the vendored installer API compatible while doing no work.

process.exit(0);
